# Conformance Tests

This directory is reserved for fixture-driven tests that assert the PHP runtime
matches the Axiom v1 specification.

The intended shape is:

- positive parsing and evaluation cases
- negative parsing and typing cases
- table artifact validation cases
- extension conformance cases once the extension model is implemented
