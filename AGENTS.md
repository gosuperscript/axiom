## Agent skills

### Issue tracker

Issues are tracked in GitHub Issues for `gosuperscript/axiom`. See `docs/agents/issue-tracker.md`.

### Triage labels

Use the canonical `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, and `wontfix` labels. See `docs/agents/triage-labels.md`.

### Domain docs

This is a single-context repository: read root `CONTEXT.md` and relevant decisions under `docs/adr/` when they exist. See `docs/agents/domain.md`.
